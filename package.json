{
  "name": "autopilot-skill",
  "version": "1.0.0",
  "private": true,
  "description": "An agent skill for autonomous development that asks the project what «done» means",
  "license": "MIT",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "verify": "npm test"
  }
}
